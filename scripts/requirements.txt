# 데이터 추출기 의존성
pandas>=2.0

# 지도 경계(SHP → GeoJSON) 변환 — 아래 '가벼운' 조합 하나면 충분합니다.
# GDAL 설치가 필요 없어 macOS(M1/M2 포함)에서 pip로 바로 설치됩니다.
pyshp>=2.3
pyproj>=3.5
shapely>=2.0    # 읍면동 → 시군구 경계 병합(디졸브). 순수 휠로 설치됨(GDAL 불필요)

# (선택) geopandas 가 이미 있으면 더 정밀하게(디졸브) 처리합니다. 없어도 됩니다.
# geopandas>=0.14
# shapely>=2.0
